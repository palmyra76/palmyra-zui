const appConfig = {
  title: "App Demo",
  apiBaseContext: '/api/data/',
  layoutBaseContext: '/api/webconfig/'
}

const layoutConfig = {
  sidebar: {
    width: "240px"
  }
};

export { appConfig, layoutConfig }